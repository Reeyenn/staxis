# Appendix E — Critic / Prioritization Notes

## Top 10 ideas (ranked)
- #27/#4 Repoint ML off the dropped plan_snapshots table to daily_logs + re-enable schedule — CODE-VERIFIED broken (demand.py:132 and supply.py:355 both read `from plan_snapshots`, a deleted table; no ml-run-inference/ml-train entries in vercel.json crons). The flagship labor-savings ROI silently returns zeros. Highest impact-to-effort of any item and a prerequisite for 5+ other ideas (#16/#19/#31/#43).
- #3/#16-blocker Pre-flight + promote the Choice Advantage map BEFORE the owner sees Step 6 — This is the single highest launch risk per project memory (CA re-map failed twice, $11-25 burned, Opus-only). Pure operational prep, near-zero build, prevents the most likely onboarding-day disaster. Do this before anything else ships.
- #1/#22/#38 Day-1 occupancy from PMS feeds, not daily_logs, on dashboard + ROI — Kills the 'demo looked great, my account is empty' letdown using data the CUA already writes on first read. Directly fixes the jarring demo-vs-reality gap that defines onboarding week.
- #13 Build the housekeeper-workflow overlay table to stop the agent reporting blind zeros — CODE-CORROBORATED (agent hardcodes in_progress/dnd/issuesFlagged=0). The flagship 'Ask Staxis' will say '0 rooms in progress' in the live walkthrough. Net-new data asset that also feeds the ML supply model.
- #45/#9 Founder-facing one-screen hotel-readiness checklist (green-light gate) — Solo non-technical founder currently scavenger-hunts across property-sessions/mapper/doctor with known doctor red-noise masking real failures. Cheap insurance that turns go-live verification into a 10-second check and scales to hotels #2-#20.
- #10/#30 Guest-message → real-operations loop (assigns crew + SLA, not just a ticket) — The one thing the entire crowded guest-AI category (Canary, Runnr, Viqal) structurally cannot do; converts Staxis's back-of-house engine into a guest-facing moat. Reuses work orders + on-shift roster + SMS pages that already exist.
- #28/#25/#40 Brand-QA closed loop: failed Choice standard → assigned, re-inspected, evidence-logged work order + QA Evidence Pack — The single highest-stakes GM fear (failed QA/PIP = franchise termination) and the sharpest wedge to DISPLACE (not just match) Quore, the Choice Qualified Vendor. CHARLIE only coaches; nobody closes finding→action→proof.
- #37 Pre-promote the shared Choice Advantage knowledge file → 'live in days, no migration' as the sales headline — Marginal onboarding cost for the 2nd+ Choice hotel approaches zero because the recipe is keyed by PMS family; directly attacks the verified #1 buyer friction (69% cite legacy-integration). Turns the hardest technical asset into the GTM wedge.
- #6 'Connect PMS later' escape hatch in the onboarding wizard — Removes the single hard wall on the critical path (bad creds / MFA / failed learn = stuck spinner). Cheap launch-week insurance so a setup hiccup never costs the whole onboarding.
- #15/#44/#50 Two-way conversational SMS routed through the 36-tool agent — CODE-VERIFIED gap (sms-reply only does language switch). Staff text 'mark 214 clean' / 'I'm sick' and the agent acts; the brain/tools/role-scoping/outbound infra all already exist, only the inbound bridge is missing. Meets the no-app frontline where they live.

## Biggest surprises (things you likely don't know)
- The flagship labor-savings ML is CODE-VERIFIED broken, not theoretically at-risk: both demand.py:132 and supply.py:355 query `from plan_snapshots` — a table deleted in the v4 rebuild — and the ML crons are absent from vercel.json. The headline 'how many housekeepers tomorrow' feature returns zeros today. This is the strongest hard-dollar pitch and it silently does not work.
- Choice already gives Comfort Suites the things several ideas implicitly position against. Core rate optimization is a LOSING fight — ChoiceMAX (IDeaS-powered) is effectively brand-provided with 9/10 franchisee adoption and 93% recs accepted. And CHARLIE (launched May 7 2026) DOES cover maintenance + guest comms + staff coaching, so any 'Choice gives you nothing' pitch is factually wrong and a savvy GM will catch it. Frame as complementary, not competing.
- The Desktop checkout is EXACTLY 298 commits behind upstream (git-verified) with core files showing as deleted. Building/QA-ing/deploying from this Mac during onboarding week could ship stale code and clobber recent live fixes (2FA, sign-in, dashboard honesty). This is the easiest way to accidentally break the live hotel — and it's a process trap, not a feature.
- An OFFICIAL data path to Choice Advantage likely exists and was nearly missed: SkyTouch /CONNECT is self-certifying, has no setup fee, and already lists a live Housekeeping interface category with four third-party vendors (incl. Quore). An official feed would be far more reliable than the CUA that has broken repeatedly on CA UI/Okta changes. Worth investigating in parallel — the strategy under-weighted this.
- Staxis can't write back to the PMS yet — onboarding must be pitched READ-ONLY. The write-back safety plumbing exists but the actual Choice Advantage room-status recipe was never authored. Note: I could NOT find a `seed-write-recipe.ts` file in the repo (only a reference in cua-service/types.ts), so the idea's claim that 'only the recipe content is a TODO template' is not fully substantiated — verify before scoping #12.
- There is a live, funded, name-collision competitor: hotelops.ai — near-identical positioning (housekeeping/maintenance/guest/compliance/multilingual). Reeyen should know this competitor exists BY NAME before any sales conversation, and lock down the Staxis brand/SEO. The strategy mentions it but it deserves founder-level attention.
- The data flywheel is hostage to one human behavior: cleaning_events only fills if housekeepers actually tap Start/Done on shared phones, and the cohort keys (brand/region/size) are currently blank for Comfort Suites — so even the day-1 cold-start estimates are weaker than advertised. Two near-zero-effort fixes (#7 + physically walking crew through taps) gate the entire ML value prop.

## Angles still worth exploring
- INVESTIGATE the official SkyTouch /CONNECT API path before relying solely on the CUA — it's self-certifying, no setup fee, and already has third-party housekeeping vendors live. An official feed would de-risk the brittle screen-scraper that has broken repeatedly on CA UI/Okta/table-anchoring. No idea in the list pursues this; it's arguably the biggest reliability lever for the whole platform.
- MFA live-resolution rehearsal is treated as an onboarding checklist item but there's no PRODUCT idea to remove the human-in-the-loop dependency (e.g. pre-trust device + alert-on-MFA-fire to the founder's phone). For hotels #2-#20 this manual babysitting doesn't scale and isn't addressed.
- Resend transactional email is OFF in production (invite emails silently fail) — flagged as a low onboarding note but there's no idea to fix it. For a self-serve 'live in days' GTM, broken invite email is a silent funnel-killer at scale.
- Model routing / cost discipline (#36/#53, pickModel() always returns Sonnet) is ranked low but is the founder's margin lever on thin sub-100-room economics. Underweighted given 30-60% potential agent-cost reduction with no UX loss.
- No idea addresses turning DOWN the cost-cap fail-open risk (the $5/day cap stops protecting if the DB read errors) — a small but real unbounded-spend tail risk during a heavy onboarding day.
- Land-and-expand portfolio pricing + 30/60/90 health dashboard (#42/#48) is the existential retention motion for a one-customer-then-scale business, yet sits mid-pack. For a solo founder who can't run a CS team manually, the churn-radar is arguably more important than several feature ideas above it.
- No idea covers the housekeeper language-button-invisible-on-white-header bug (noted in project memory as a literal frontline activation blocker) — a tiny fix gating the data flywheel that the strategy's own onboarding notes call out.

## Ideas the critic flagged as weak/generic
- #21/#33/#24 split predictive-maintenance/warranty/CapEx into three near-identical ideas — they're one feature bundle (the repo already stores per-asset cost/warranty + has a CapEx tab, so it's mostly plumbing). Should be merged; listing thrice inflates the backlog.
- #17/#23/#29 operations-aware upsell appears three times with nearly identical justification. One idea. Also lower-priority for THIS customer: upsell TAM at a 74-room limited-service Comfort Suites is modest, and it has zero onboarding-week relevance.
- #7 Seed brand/region/size — correct and near-zero-effort, but it's a data-entry checklist line, not a 'feature'; belongs in the onboarding runbook (#45's gate), not the idea backlog.
- #52 stale-checkout guardrail — important but it's a one-time process rule + a git pre-check, not a product idea; it's really part of the build-process hygiene, not a ranked initiative.
- #35/#39 CHARLIE-complementary positioning is a GTM one-pager, valuable but it's messaging, not product. Don't let it compete for build capacity with the broken-ML or empty-dashboard fixes.
- #26 turnover/burnout early-warning — genuinely differentiated ROI story but depends entirely on cleaning_events/attendance data that is currently ZERO rows; it's months out and shouldn't rank near launch-critical items.
- #34 structured engineering readings — legitimately a Quore-parity gap (verified: PreventiveTab uses freeform Area) but lowest-glamour and not onboarding-week-blocking; right to keep, wrong to treat as high-impact for the first deal.
- #5/#48/#42 first-72-hours checklist + 30/60/90 health dashboard overlap heavily; consolidate into one activation-instrumentation workstream.

## ⚠️ Claims to double-check before relying on them
- '/feed is sample-data only, just needs binding to existing engines' (#8/#11) — I could NOT find any /feed page or component under src/app. Either it doesn't exist yet (so this is build-new, not wire-up) or it's named differently. The 'far less build than it looks' framing rests on an unverified premise. Verify the surface exists before scoping.
- 'Write-back safety plumbing fully built; only the recipe content is a TODO placeholder in seed-write-recipe.ts' (#12) — no seed-write-recipe.ts file exists in the repo; only a reference in cua-service/src/types.ts. The 'lowest-hanging fruit, just author one screen' claim is not substantiated. Could be significantly more work.
- Vendor ROI percentages quoted as near-fact throughout (Optii 15% labor / 3-5 min per room / 500% ROI; Canary 10x upsell; Ritz 20% faster turns + 41% fewer emergency calls; 3-5x AI upsell conversion; 5-15% labor savings). The market lenses themselves flag every one of these as vendor/case-study marketing, not audited. Do not put them in a sales deck as guarantees.
- OTA-commission-deflection dollar figures ($45-75k/yr) are sized for a 200-room hotel; Comfort Suites Beaumont is ~74 rooms. Any quoted savings must be scaled down ~3x or it overpromises.
- 'Ancillary is ~20-30% of revenue at mature properties' — the source range is actually 10-40% depending heavily on segment; a limited-service Comfort Suites with a few suite tiers is at the low end, not 20-30%. The upsell TAM for THIS customer is smaller than the headline implies.
- Pricing recommendations ($7-12/room, $199-299 floor) are the strategy's own synthesis, validated against NO competitor for an AI-native limited-service OPS suite (this category is genuinely new). Treat as a starting hypothesis to test, not a defensible number.
- 'CPOR $46.79 / room-attendant MPOR 23.91' are ALL-HOTELS medians; select-service runs leaner. Benchmarking Comfort Suites against the inflated full-service average would make Staxis's labor numbers look artificially good and mislead the GM.
- Choice Advantage franchise tier likely never exposes revenue/ADR/RevPAR — so any revenue-analytics or margin-dashboard idea (Owner Margin view) may render permanently empty for this customer. Lead value story with labor + inventory, not revenue.

