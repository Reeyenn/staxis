# Bumped 2026-05-20 as part of Phase 2 security hardening — see plan §4
# Conservative: web stack + security-relevant libs upgraded. Scientific
# stack stays exact-pinned; scikit-learn 1.5.0 passed the full suite plus
# cross-version Bayesian/XGBoost artifact loads from the previous 1.3.2 runtime.
# FastAPI 0.139.0 allows the patched Starlette 1.x line; 0.115.0 pinned
# Starlette 0.38.x, which now carries multiple request-handling advisories.
fastapi==0.139.0
starlette==1.3.1
uvicorn==0.32.0
pydantic==2.9.2
pydantic-settings==2.6.0
python-dotenv==1.2.2
supabase==2.10.0
xgboost~=2.1.0
scikit-learn==1.5.0
pandas==2.1.3
numpy==1.26.4
scipy==1.11.4
psycopg2-binary==2.9.9
requests==2.33.0
# pytest-asyncio 1.3.0 is the first stable release with pytest 9 support.
pytest==9.0.3
pytest-asyncio==1.3.0
# Plan v2 Phase 2 — fleet-wide rate limit for the FastAPI surface. Without
# this, a leaked bearer token would let a caller hammer /predict and
# /train endpoints and rack up Railway CPU. 60/min/IP is generous for
# the cron-driven workload (we make at most a few per-property calls
# per day) and tight enough to bound an abuse scenario.
slowapi==0.1.9
# Sentry error monitoring (2026-05-22 — monitoring/logging/secrets hardening).
# Plain sentry-sdk (no [fastapi] extra) — the FastAPI integration auto-
# captures handled HTTPException + RateLimitExceeded as errors, which
# would flood the dashboard with expected 4xx noise. The general
# exception handler in main.py captures unhandled errors explicitly.
# Pinned exact to match the file's existing convention.
sentry-sdk==2.17.0
